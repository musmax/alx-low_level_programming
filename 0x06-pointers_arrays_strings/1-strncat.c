#include "main.h"
/**
 * main enrty
 *  a function that concatenates two strings.
 *  return: 0 (success/correct)
 */
char *_strncat(char *dest, char *src, int n)
{int count;
n = 0;
while (*(dest + n) != '\0'){
n++;
while (*(dest + n) = *(src + count))
{
	count++;
	n++;
	if (count == '\0')
	{
		break;
	}
}
}
